import React, { Component } from 'react';
import PropTypes from 'prop-types';
import "./dialog.scss"

export default class Dialog extends Component {
    render() {
        return <div className="hp-dialog"></div>
    }
}